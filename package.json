{
  "name": "my-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd src && cd backend && node server.js\" \"cd src && cd frontend && npm start\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
